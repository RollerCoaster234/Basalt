Todo:

Important:

- Fix Textfield -> Inside a sub-sub frame the cursor vertical coordinates have wrong position (by 1)
- Image is not finished
- Flexbox has a bug when resizing the window multiple times (it starts to look wrong after 4-5x)
- Check behaviour with scrollable container inside another scrollable container
- program env is not safe
- remove dead threads
- change how errors are shown

Low:

- Skip some render calls
- Better logging system
- Better debugging tools
